# Generative Layers — Override for Jason's default logging.properties
# Replaces MASConsoleLogHandler (only in jason-cli) with standard ConsoleHandler.

handlers = java.util.logging.ConsoleHandler

.level = INFO

java.util.logging.ConsoleHandler.level = FINE
java.util.logging.ConsoleHandler.formatter = java.util.logging.SimpleFormatter

java.level=OFF
javax.level=OFF
sun.level=OFF
jade.level=OFF
