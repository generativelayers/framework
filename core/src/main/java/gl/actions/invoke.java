package gl.actions;
/**
 * Short-path alias: {@code gl.actions.invoke} so Jason agents
 * can write {@code gl.actions.invoke(...)} instead of
 * {@code gl.adapter.jason.actions.invoke(...)}.
 */
public class invoke extends gl.adapter.jason.actions.invoke {}
